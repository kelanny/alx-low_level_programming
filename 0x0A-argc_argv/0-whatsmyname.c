#include "main.h"
#include <stdio.h>

/**
 * main - The program prints its name
 * @argc: input count variable
 * @argv: input vector variable
 *
 * Return: Always 0 (Success)
 */

int main(int argc, char *argv[])
{
	(void) argc;
	printf("%s\n", argv[0]);

	return (0);
}
