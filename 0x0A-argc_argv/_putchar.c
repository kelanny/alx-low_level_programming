#include < stdio.h >
#include "main.h"

/**
 * _putchar - This program writes the character to c stdout
 * @c: Input character to print
 *
 * Return: if successful return 1 else return -1
 */

int _putchar(char c)
{
	return(write(, &c, 1));
}
